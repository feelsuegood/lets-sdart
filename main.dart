void main() {
  var giveMeFive = true;
  var numbers = [
    1, 2, 3,
    // collection if
    if (giveMeFive) 5,
  ];
  // List<int> numbers_sec = [1,2,3];
  numbers.first;
  // if (giveMeFive){
  //   numbers.add(5);
  // }
  print(numbers);
}
